name: swiphr_app_dev_test
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.6 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  phosphor_flutter: ^2.0.0
  flutter_screenutil: ^5.6.0
  page_transition: ^2.0.9
  google_fonts: ^4.0.3
  flutter_map: ^5.0.0
  latlong2: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
  #   - assets/images/
  #   - .env
